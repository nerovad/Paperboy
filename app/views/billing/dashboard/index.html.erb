<div class="form-header">
  <h1>Billing</h1>
</div>

<div class="form-wrapper">
  <% if billing_tool_sections.any? %>
    <p>
      The billing tools are in the sidebar. Each button asks for confirmation —
      and a start and end date where the process needs one — before it runs its
      stored procedure against GSABSS.
    </p>
  <% else %>
    <p>Welcome to Billing.</p>
  <% end %>
</div>
