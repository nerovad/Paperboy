<%= turbo_stream.update("unit-select") do %>
  <%= select_tag "parking_lot_submission[unit]",
        options_for_select(@unit_options, window.prefillData?.unit),
        class: "form-control",
        id: "unit-select",
        data: { gsabss_selects_target: "unit" } %>
<% end %>
