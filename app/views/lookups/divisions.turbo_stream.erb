<%= turbo_stream.update("division-select") do %>
  <%= select_tag "parking_lot_submission[division]",
        options_for_select(@division_options, window.prefillData?.division),
        class: "form-control",
        id: "division-select",
        data: { gsabss_selects_target: "division", action: "change->gsabss-selects#loadDepartments" } %>
<% end %>

