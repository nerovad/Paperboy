<div class="form-header">
  <h1>Print 2 Mail</h1>
</div>

<div class="form-wrapper">
  <p>Hello World! This is the starting page for Print 2 Mail.</p>
  <p>
    Replace this view (<code>app/views/p2m/dashboard/index.html.erb</code>) with
    the real thing, and add pages by creating controllers under
    <code>app/controllers/p2m/</code> that inherit from
    <code>P2m::BaseController</code>.
  </p>
  <p>
    The app's root path is the slideshow landing page
    (<code>app/views/p2m/dashboard/home.html.erb</code>); this dashboard is
    one level in, matching how AIM and Digital Asset Management are laid out.
  </p>
</div>
